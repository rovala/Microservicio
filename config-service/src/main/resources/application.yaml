server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/rovala/Microservicio
          search-paths: config-data
          username: rovalasiul@gmail.com
          password: juna1308@
  application:
    name: config-service